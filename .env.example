# Storage mode — local uses localStorage, cloud uses Supabase (Phase 3)
VITE_STORAGE_MODE=local

# Supabase config (Phase 3 only — leave blank for local mode)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
